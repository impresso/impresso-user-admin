Dear {{ user.get_full_name|default:user.username }},
Dear {{ reviewer.get_full_name|default:reviewer.username }},

This is an automatic notification from the Impresso User Management and Data Access system.

We wish to inform you that {{ user.get_full_name|default:user.username }} has requested special membership access to the {{ user_special_membership_request.subscription.title }}.

Here are the details of the access request for your reference:

    Requester: {{ user.get_full_name|default:user.username }}
    Requester Email: {{ user.email }}
    Requester Plan: {{ plan_label }}
    Number of Special Membership Accesses granted: {{ number_of_special_memberships }}

    Affiliation: {% if user.profile.affiliation %}{{ user.profile.affiliation }}{% else %}Not provided{% endif %}
    Public Institutional Webpage: {% if user.profile.institutional_url %}{{ user.profile.institutional_url }}{% else %}Not provided{% endif %}
    
    Membership Option: {{ user_special_membership_request.subscription.title }}
    Request Date: {{ user_special_membership_request.date_created|date:"F j, Y, g:i a" }}
    Request Status: Pending Review

    Notes: 
    {% if user_special_membership_request.notes %}{{ user_special_membership_request.notes }}{% else %}No notes provided.{% endif %}

We have included both parties in this message to facilitate direct communication. {{ user.first_name|default:user.username }}, please feel free to reach out to {{ reviewer.get_full_name|default:reviewer.username }} here for any questions regarding your request or the data access process.

Should you need any further assistance from our side, the Impresso Team remains at your service.

Warm regards,
The Impresso Team